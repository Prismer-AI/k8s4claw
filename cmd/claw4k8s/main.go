package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Println("claw4k8s companion runtime — starting")
	// TODO: implement K8s client, escalation watcher, analysis pipeline
	fmt.Fprintln(os.Stderr, "claw4k8s: not yet fully implemented")
	os.Exit(0)
}
